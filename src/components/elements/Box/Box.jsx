import styled from "styled-components/native";
import {
  layout,
  border,
  shadow,
  position,
  space,
  color,
  background,
  grid,
} from "styled-system";

export const MobBox = styled.View`
  ${layout};
  ${border};
  ${shadow};
  ${position};
  ${space};
  ${color};
  ${background};
  ${grid};
`;
